import { AppNavigation } from "../SectionNavigation/SectionNavigation";
import "./Header.scss";

export const Header = () => {
  return (
    <section className="header">
      <AppNavigation />
    </section>
  );
};
