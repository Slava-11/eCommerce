import { AppNavigation } from "../AppNavigation/AppNavigation";
import "./Header.scss";

export const Header = () => {
  return (
    <section className="header">
      <AppNavigation />
    </section>
  );
};
